document.addEventListener('DOMContentLoaded',()=>{const html=document.documentElement,main=document.querySelector('main');if(!main)return;const scenes=[...main.querySelectorAll(':scope>section')];if(!scenes.length)return;html.classList.add('pathway-mode');document.body.classList.add('pathway-mode');scenes.forEach((s,i)=>{s.classList.add('path-scene');s.dataset.pathIndex=i;s.dataset.pathTitle=(s.querySelector('.section-number')?.textContent||s.querySelector('h1,h2')?.textContent||`Chapter ${i+1}`).replace(/\s+/g,' ').trim()});const names=scenes.map((s,i)=>(s.dataset.pathTitle||'').split('·').pop().trim().slice(0,18)||`Chapter ${i+1}`);let index=0,busy=false,wheelLock=false;const ui=document.createElement('div');ui.innerHTML=`<div class="path-route" id="pathRoute"><div class="path-route-line"></div><div class="path-route-progress" id="pathProgress"></div></div><div class="path-counter"><strong id="pathCurrent">01</strong> <span>/ ${String(scenes.length).padStart(2,'0')} · <span id="pathName">${names[0]}</span></span></div><div class="path-hint"><kbd>←</kbd><kbd>→</kbd> navigate · <kbd>space</kbd> continue</div><button class="path-back" id="pathBack" type="button">← Back</button><button class="path-next pulse" id="pathNext" type="button">Continue <span>→</span></button><div class="path-character" id="pathCharacter"><img src="assets/boss-sky.webp" alt="Boss in Sky"><span class="char-fallback">♡</span></div><div class="path-toast" id="pathToast"></div>`;document.body.appendChild(ui);const route=document.getElementById('pathRoute'),progress=document.getElementById('pathProgress'),current=document.getElementById('pathCurrent'),nameEl=document.getElementById('pathName'),next=document.getElementById('pathNext'),back=document.getElementById('pathBack'),character=document.getElementById('pathCharacter'),toastEl=document.getElementById('pathToast');scenes.forEach((s,i)=>{const node=document.createElement('button');node.className='path-node';node.type='button';node.dataset.index=i;node.innerHTML=`<span>${names[i]}</span>`;node.setAttribute('aria-label',`Go to ${names[i]}`);route.appendChild(node);node.addEventListener('click',()=>go(i))});const nodes=[...route.querySelectorAll('.path-node')];function setClasses(){scenes.forEach((s,i)=>{s.classList.toggle('is-active',i===index);s.classList.toggle('is-before',i<index);s.classList.toggle('is-after',i>index)});nodes.forEach((n,i)=>{n.classList.toggle('active',i===index);n.classList.toggle('done',i<index)});current.textContent=String(index+1).padStart(2,'0');nameEl.textContent=names[index];const pct=scenes.length<2?0:index/(scenes.length-1)*96;progress.style.height=`${pct}%`;const rh=route.clientHeight*.96;character.style.top=`calc(2% + ${rh*(index/Math.max(1,scenes.length-1))}px)`;back.style.opacity=index===0?'0':'1';back.style.pointerEvents=index===0?'none':'auto';next.innerHTML=index===scenes.length-1?'Restart <span>↻</span>':'Continue <span>→</span>';next.classList.toggle('pulse',index!==scenes.length-1)}function burst(){if(typeof window.burstHearts==='function')window.burstHearts(8);for(let i=0;i<5;i++){const e=document.createElement('span');e.textContent=['✦','♡','✧'][i%3];e.className='floating-sparkle';e.style.left=`${40+Math.random()*20}%`;e.style.top=`${45+Math.random()*15}%`;document.getElementById('sparkles')?.appendChild(e);setTimeout(()=>e.remove(),7000)}}function toast(text){toastEl.textContent=text;toastEl.classList.add('show');clearTimeout(toastEl._t);toastEl._t=setTimeout(()=>toastEl.classList.remove('show'),1700)}function go(to){if(busy)return;if(to<0)to=0;if(to>=scenes.length)to=scenes.length-1;if(to===index){if(index===scenes.length-1){index=0;setClasses();burst();toast('Let’s tell the story again ♡')}return}busy=true;index=to;setClasses();burst();if(index===scenes.length-1)toast('And this is where I finally tell you.');else if(index===4)toast('Some memories are quiet on purpose.');setTimeout(()=>busy=false,700)}function advance(){if(index===scenes.length-1){go(0);return}go(index+1)}next.addEventListener('click',advance);back.addEventListener('click',()=>go(index-1));document.addEventListener('keydown',e=>{if(['INPUT','TEXTAREA'].includes(document.activeElement?.tagName))return;if(e.key==='ArrowRight'||e.key==='ArrowDown'||e.key===' '){e.preventDefault();advance()}else if(e.key==='ArrowLeft'||e.key==='ArrowUp'){e.preventDefault();go(index-1)}else if(e.key==='Home'){e.preventDefault();go(0)}else if(e.key==='End'){e.preventDefault();go(scenes.length-1)}});document.addEventListener('wheel',e=>{if(Math.abs(e.deltaY)<18||wheelLock)return;wheelLock=true;e.deltaY>0?advance():go(index-1);setTimeout(()=>wheelLock=false,950)},{passive:true});let sx=0;document.addEventListener('touchstart',e=>sx=e.changedTouches[0].clientX,{passive:true});document.addEventListener('touchend',e=>{const dx=e.changedTouches[0].clientX-sx;if(Math.abs(dx)>55)dx<0?advance():go(index-1)},{passive:true});const enter=document.getElementById('enterBtn');enter?.addEventListener('click',e=>{e.preventDefault();advance();burst()},{capture:true});const img=character.querySelector('img');img?.addEventListener('error',()=>img.style.display='none');window.addEventListener('resize',setClasses);setClasses()});