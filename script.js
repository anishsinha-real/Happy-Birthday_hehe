const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const track=$('#track'),slides=$$('.slide'),dots=$('#dots'),progress=$('#progressBar'),label=$('#progressLabel');let index=0;let dragging=false,startX=0,startTranslate=0;
function goTo(n){index=Math.max(0,Math.min(slides.length-1,n));track.style.transform=`translate3d(${-index*100}vw,0,0)`;slides.forEach((s,i)=>s.classList.toggle('active',i===index));progress.style.width=`${((index+1)/slides.length)*100}%`;label.textContent=`${String(index+1).padStart(2,'0')} / ${String(slides.length).padStart(2,'0')}`;dots.querySelectorAll('.dot').forEach((d,i)=>d.classList.toggle('active',i===index));if(index>0)$('#swipeHint').style.opacity='.25'}
slides.forEach((_,i)=>{const d=document.createElement('button');d.className='dot';d.setAttribute('aria-label',`Go to slide ${i+1}`);d.onclick=()=>goTo(i);dots.appendChild(d)});
$('#next').onclick=()=>goTo(index+1);$('#prev').onclick=()=>goTo(index-1);$$('[data-next]').forEach(b=>b.onclick=()=>goTo(index+1));$('#restart').onclick=()=>{goTo(0);burst(18)};
window.addEventListener('keydown',e=>{if(['ArrowRight','PageDown'].includes(e.key))goTo(index+1);if(['ArrowLeft','PageUp'].includes(e.key))goTo(index-1)});
const carousel=$('#carousel');carousel.addEventListener('pointerdown',e=>{if(e.target.closest('button'))return;dragging=true;startX=e.clientX;startTranslate=-index*innerWidth;track.style.transition='none';carousel.setPointerCapture(e.pointerId)});carousel.addEventListener('pointermove',e=>{if(!dragging)return;track.style.transform=`translate3d(${startTranslate+e.clientX-startX}px,0,0)`});carousel.addEventListener('pointerup',e=>{if(!dragging)return;dragging=false;track.style.transition='';const dx=e.clientX-startX;if(Math.abs(dx)>55)goTo(index+(dx<0?1:-1));else goTo(index)});carousel.addEventListener('pointercancel',()=>{dragging=false;track.style.transition='';goTo(index)});
carousel.addEventListener('wheel',e=>{if(Math.abs(e.deltaX)>Math.abs(e.deltaY)||Math.abs(e.deltaY)>45){e.preventDefault();clearTimeout(window.wheelTimer);window.wheelTimer=setTimeout(()=>goTo(index+(e.deltaY>0||e.deltaX>0?1:-1)),70)}},{passive:false});
let music=$('#bgMusic'),musicBtn=$('#soundToggle'),musicPaused=false;music.volume=.42;async function playMusic(){try{await music.play();musicBtn.classList.add('active');musicBtn.textContent='♫';musicPaused=false}catch(e){}}musicBtn.onclick=async()=>{if(music.paused){await playMusic()}else{music.pause();musicPaused=true;musicBtn.classList.remove('active');musicBtn.textContent='×'}};document.addEventListener('pointerdown',()=>{if(!musicPaused&&music.paused)playMusic()},{once:true});
for(let i=0;i<120;i++){const s=document.createElement('span');s.className='star';s.style.left=Math.random()*100+'%';s.style.top=Math.random()*100+'%';s.style.animationDelay=Math.random()*4+'s';$('#stars').appendChild(s)}
function heart(symbol){const h=document.createElement('span');h.className='heart';h.textContent=symbol||['♡','♥','✦','✧'][Math.floor(Math.random()*4)];h.style.left=Math.random()*100+'%';h.style.bottom='-30px';h.style.fontSize=12+Math.random()*14+'px';h.style.animationDuration=5+Math.random()*4+'s';$('#hearts').appendChild(h);setTimeout(()=>h.remove(),9000)}function burst(n=12){for(let i=0;i<n;i++)setTimeout(()=>heart('♡'),i*45)}setInterval(()=>heart(),1200);
const envelope=$('#envelope'),letter=$('#letterContent');envelope.onclick=()=>{const open=envelope.classList.toggle('open');letter.classList.toggle('open',open);letter.setAttribute('aria-hidden',String(!open));$('#envelopeHint').textContent=open?'♡ From me to you.':'Tap the envelope.';if(open){burst(18);setTimeout(()=>letter.scrollIntoView({behavior:'smooth',block:'center'}),400)}};
$('#yesBtn').onclick=()=>{$('#answerMessage').textContent='Okay… then I guess I officially have permission to keep falling for you. ♡';$('#yesBtn').textContent='Best. Answer. Ever. ♡';$('#yesBtn').disabled=true;burst(36)};$('#hugBtn').onclick=()=>{$('#answerMessage').textContent='Come here then, Boss. 🫂♡';burst(25)};
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});$$('.reveal').forEach(e=>observer.observe(e));
goTo(0);setTimeout(()=>$('#swipeHint').style.opacity='.8',900);setTimeout(()=>$('#swipeHint').style.opacity='.25',6500);
