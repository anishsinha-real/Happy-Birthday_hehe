const layers=[...document.querySelectorAll('.layer')];const progress=document.getElementById('progressBar');let current=0;let busy=false;const show=i=>{if(i<0||i>=layers.length||busy)return;busy=true;layers.forEach((l,n)=>l.classList.toggle('active',n===i));current=i;progress.style.width=`${((i+1)/layers.length)*100}%`;setTimeout(()=>busy=false,760)};document.querySelectorAll('.next').forEach(b=>b.addEventListener('click',()=>show(current+1)));document.getElementById('restart').addEventListener('click',()=>show(0));const yes=document.getElementById('yesBtn');const think=document.getElementById('thinkBtn');const endingEyebrow=document.getElementById('endingEyebrow');const endingTitle=document.getElementById('endingTitle');const endingText=document.getElementById('endingText');yes.addEventListener('click',()=>{endingEyebrow.textContent='You said yes. ♡';endingTitle.innerHTML="Then let's make<br><em>some memories.</em>";endingText.textContent="Happy birthday, Clara. I think this might be my favourite birthday wish I've ever made.";show(6);burst()});think.addEventListener('click',()=>{endingEyebrow.textContent='Take all the time you need. ♡';endingTitle.innerHTML="No pressure.<br><em>Really.</em>";endingText.textContent="I just wanted you to know how I feel. Whatever your answer is, I'm grateful I got to meet you.";show(6)});let touchStart=0;window.addEventListener('wheel',e=>{if(Math.abs(e.deltaY)<25)return;e.deltaY>0?show(current+1):show(current-1)},{passive:true});window.addEventListener('touchstart',e=>touchStart=e.touches[0].clientY,{passive:true});window.addEventListener('touchend',e=>{const d=touchStart-e.changedTouches[0].clientY;if(Math.abs(d)>45)d>0?show(current+1):show(current-1)},{passive:true});document.addEventListener('keydown',e=>{if(['ArrowDown','PageDown',' '].includes(e.key)){e.preventDefault();show(current+1)}if(['ArrowUp','PageUp'].includes(e.key)){e.preventDefault();show(current-1)}});const stars=document.getElementById('stars');for(let i=0;i<80;i++){const s=document.createElement('i');s.className='star';s.style.left=Math.random()*100+'%';s.style.top=Math.random()*100+'%';s.style.animationDelay=Math.random()*4+'s';s.style.opacity=(.15+Math.random()*.5).toFixed(2);stars.appendChild(s)}const soundBtn=document.getElementById('soundBtn');let audioCtx;let muted=true;soundBtn.addEventListener('click',()=>{muted=!muted;soundBtn.querySelector('span').textContent=muted?'sound':'on';if(!muted){audioCtx=audioCtx||new AudioContext();const o=audioCtx.createOscillator(),g=audioCtx.createGain();o.frequency.value=220;g.gain.value=.015;o.connect(g);g.connect(audioCtx.destination);o.start();o.stop(audioCtx.currentTime+.25)}});function burst(){for(let i=0;i<28;i++){const s=document.createElement('i');s.className='star';s.style.left='50%';s.style.top='50%';s.style.position='fixed';s.style.transition='1.2s ease';s.style.zIndex=50;document.body.appendChild(s);requestAnimationFrame(()=>{const a=Math.random()*Math.PI*2,r=120+Math.random()*280;s.style.transform=`translate(${Math.cos(a)*r}px,${Math.sin(a)*r}px) scale(2)`;s.style.opacity='0'});setTimeout(()=>s.remove(),1300)}}
